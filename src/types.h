#ifndef TYPES_H
#define TYPES_H

enum Type
{
        INT,
        REAL_TYPE,
        AINT,
        AREAL,
        BOOL,
        PG_NAME,
        PG_PARM,
        PROC,
        OK,
        UNDEC,
        ERR
};

#endif
